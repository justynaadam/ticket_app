<% if current_user && @search.present? && @search.user.nil? %>
  <%= form_for @search do |f| %>
    <div><%= hidden_field_tag :user_id, current_user.id %></div>
    <%= f.submit "save search", class: "btn btn-primary" %>
  <% end %>
<% end %>